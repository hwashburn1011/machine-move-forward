from pathlib import Path
p=Path('assets/gunner-s07/viewer/index.html');s=p.read_text(encoding='utf-8').replace('<style>', '<script type="importmap">{"imports":{"three":"./vendor/three.module.js","three/addons/":"./vendor/addons/"}}</script><style>',1);p.write_text(s,encoding='utf-8')
p=Path('assets/gunner-s07/README.md');s=p.read_text(encoding='utf-8').replace('The local interactive viewer is at `http://127.0.0.1:5193/assets/gunner-s07/viewer/` while the project\'s Vite server is running.', 'The local interactive viewer is at `http://127.0.0.1:5195/viewer/` while its preview server is running. The package includes Three.js and its MIT license, so it needs no online asset service. After extracting the ZIP, run `python -m http.server 8000 --bind 127.0.0.1` from the package folder and open `http://127.0.0.1:8000/viewer/` to use it again.');p.write_text(s,encoding='utf-8')
